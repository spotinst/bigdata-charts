{{- if .Values.telemetry.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "bigdata-spark-watcher.fullname" . }}-telemetry-cm
  labels:
  {{- include "bigdata-spark-watcher.labels" . | nindent 4 }}
data:
  spark_watcher_filters.conf: |
{{ .Values.telemetry.fluentbit.configMap.config | toString | indent 6 }}
{{- end }}

